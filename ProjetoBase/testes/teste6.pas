program cmdIf (input, output);
var i, j: integer;
begin;
    i:=0;
    while (i < j) do
    begin
        if (i div 2 * 2 = i)
            then j:=20;
            else j:=i+20;
        i := i+1
    end;
end.